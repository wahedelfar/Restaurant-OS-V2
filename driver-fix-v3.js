(function(){
  'use strict';
  // Deprecated compatibility file. Canonical driver implementation is in delivery-gps-clean-v2.js.
  // Intentionally does not render a second driver UI or make RPC calls.
  if(window.__ROS_DRIVER_FIX_SHIM__) return;
  window.__ROS_DRIVER_FIX_SHIM__=true;
})();
